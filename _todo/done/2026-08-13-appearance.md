

- the week schedule is always scrollable it should only scroll when window is narrow
- on safari, changing the time of day looks bad. I think when editing the input needs a background / to be on a pill instead of transparent
- add some instructions to "My activities" modal. It should explain that these are used for a typical week
- in the copy week modal make the first option "My activities"
- when no activities in a week, make there two placeholders. one is the "Add new activity" (replace the +), the other is purple and first (to the left) and it should say "Add my activities"
- Make "My activities" should be called "My weekly activities"
- Remove the dumbbell icon from the header button