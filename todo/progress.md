
# Progressing

This app is built on the idea that someone is maintaining. 
I also want this app to be helpful for someone who is increasing their mileage / activity.

To do this the interface needs to have a default week that the user pre-defines.

- In the top left of the header, add a glowing button for "My Week".
- This will open a modal with the same features as the current sidebar (without progress percent)

Then a workout week that can be changed on the fly.

- In the schedule, above the days of each week, add a side-scrollable list of all the workout for this week
- the list items are smaller cards that show the progress percent and have draggable tags (the same as the ones in the sidebar)
- this should be available on every week

That means we should remove the sidebar.

- remove the sidebar